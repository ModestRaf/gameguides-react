import React from 'react';
import styles from './Footer.module.scss'; // Импортируем стили

const Footer: React.FC = () => {
    return <footer className={styles.footer}></footer>;
};

export default Footer;